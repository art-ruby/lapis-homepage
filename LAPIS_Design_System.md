# LAPIS Design System
### Prepared as a Design Director review — Apple × Airbnb design lens
### For use in Claude Design and Claude Code

---

## 0. Reference Analysis (UX/UI 관점)

여섯 개 사이트(Tamburins, Byredo, Maison Margiela, Le Labo, Diptyque, Aesop)를 화면 구조, 타이포그래피, 모션, 커머스 UX 관점에서 비교했다. 목표는 모방이 아니라 "럭셔리 향수 카테고리에서 반복되는 문법"과 "브랜드별 차별점"을 분리해내는 것이다.

### 0.1 공통 디자인 언어 (Category Convention)

| 요소 | 공통 패턴 |
|---|---|
| 레이아웃 | 풀블리드 히어로 + 넉넉한 여백. 정보 밀도를 의도적으로 낮춤 |
| 타이포 | 브랜드명은 세리프/디스플레이 서체, 본문은 얇은 산세리프로 대비 |
| 컬러 | 무채색/저채도 베이스 + 단일 포인트 컬러. 다색 사용 없음 |
| 내비게이션 | 스크롤 방향에 반응(숨김/노출)하는 미니멀 스티키 내비 |
| 커머스 | 가격·구매보다 무드·스토리를 먼저 배치. 제품 그리드는 뒤로 미룸 |
| 모션 | 느리고 조용한 트랜지션. 탄성(bounce)·과장된 이징 없음 |
| 사진 | 에디토리얼 톤. 카탈로그성 정면 누끼컷 최소화 |

### 0.2 브랜드별 차별점

**Tamburins** — 가장 실험적. 비대칭 그리드, 큰 크롭 타이포, 전시장/설치미술에 가까운 스크롤 연출. "이상 미학(anomalous beauty)"이라는 브랜드 언어를 UI 파격으로 번역함. 한국 브랜드 중 유일하게 미니멀리즘을 깨는 쪽으로 승부.

**Byredo** — 가장 절제됨. 그리드가 완벽에 가까움. 제품 사진이 작고 조용하게 배치되고, 모션도 최소. 디자인 시스템 그 자체로 읽히는 사이트. LAPIS의 "그리드 원칙" 기준점으로 삼기 좋음.

**Le Labo** — 미니멀리즘 대신 "질감"을 택함. 크래프트지, 도장, 손글씨 라벨 같은 불완전함을 럭셔리 신호로 사용. 도시 한정판·각인 등 개인화 UX가 강함. LAPIS에는 직접 차용하지 않되, "완벽하지 않은 촉감"이라는 태도는 참고할 만함.

**Diptyque** — 회화적·일러스트레이션 기반. 오벌 로고, 보태니컬 드로잉, 헤리티지 내러티브가 강함. 따뜻하고 레이어드된 톤으로, 나머지 5개 브랜드보다 상대적으로 장식성이 높음.

**Aesop** — 가장 에디토리얼/문학적. 텍스트 밀도가 높지만 조판이 정교해 답답하지 않음. 성분·철학을 설명하는 구조화된 콘텐츠 그리드. 인쇄물에 가까운 절제.

**Maison Margiela (Replica)** — 가장 시네마틱. 향 하나하나에 "기억"이라는 서사를 부여하고, 풀스크린 영상 스크롤로 몰입시킴. 여섯 브랜드 중 캠페인 필름에 가장 가까운 문법.

### 0.3 LAPIS의 포지셔닝

LAPIS는 이미 확정된 브랜드 자산이 있다 — 미드나잇 블루 + 웜 골드, 한복 실루엣 기반 실루엣, "Quiet Luxury", 캠페인명 *The Muse of Lapis*, 태그라인 "깊고 푸른 우아함". 이를 기준으로:

- **그리드/절제 원칙**은 Byredo에서
- **스크롤 기반 시네마틱 내러티브 구조**는 Maison Margiela에서
- **한국적 헤리티지를 UI 파격으로 다루는 태도**는 Tamburins에서

세 가지를 뼈대로 가져오되, 표면 스타일(컬러/서체/모티프)은 100% LAPIS 고유 자산으로 채운다. Le Labo의 질감 신호, Diptyque의 일러스트 헤리티지, Aesop의 에디토리얼 조판은 "참고 태도"로만 남기고 직접 차용하지 않는다.

---

## 1. Brand Philosophy

- **브랜드명**: LAPIS · **캠페인명**: The Muse of Lapis
- **톤앤매너**: Quiet Luxury — 조용하지만 확신에 찬 존재감
- **컬러 아이덴티티**: 미드나잇 블루(사파이어) × 웜 골드, 시네마틱 로우키 조명
- **핵심 메시지**: "LAPIS는 단순한 향기가 아니라, 말보다 먼저 공기 속에 남는 조용한 존재감이다."
- **태그라인**: 깊고 푸른 우아함
- **타겟**: 절제된 고급감과 조용한 존재감을 선호하는 20대 후반–30대 초반 여성
- **USP**: 사파이어 블루 비주얼과 차분하고 선명한 존재감을 결합한 프리미엄 향수. 한국적 헤리티지(한복 실루엣)와 서양 럭셔리 조향 문법의 교차점

디자인은 이 철학을 "설명"하지 않고 "체험"하게 만드는 것을 목표로 한다. 카피는 줄이고, 여백·조명·모션이 메시지를 대신 전달한다.

---

## 2. Design Principles

1. **Restraint is luxury** — 화려함을 더하지 말고 뺄 것. 요소 하나를 추가하기 전에 두 개를 뺄 수 있는지 먼저 검토한다.
2. **One accent rule** — 한 화면에 골드는 하나의 역할(강조)만 한다. 골드가 배경이 되는 순간 절제가 깨진다.
3. **Silence before sound** — 텍스트보다 이미지·여백·모션이 먼저 말한다. 카피는 화면당 한 문장을 넘기지 않는다.
4. **Consistency over novelty** — 화려한 트랜지션 하나보다, 모든 화면에서 동일하게 느껴지는 조용한 리듬이 우선이다.
5. **Heritage as structure, not decoration** — 한복 실루엣·청금석 모티프는 장식적 패턴이 아니라 레이아웃과 컷팅의 구조적 근거로 사용한다.
6. **Every pixel earns its place** — Apple/Airbnb 기준: 여백도 디자인 결정이다. 의도 없는 여백과 의도한 여백을 구분해 문서화한다.

---

## 3. Color Tokens

```css
:root {
  /* Core */
  --lapis-deep:   #0A1A3C; /* 메인 배경, 딥 네이비 블루 */
  --lapis-mid:    #1E3A6E; /* 섹션 구분, 그라데이션 중간톤 */
  --black:        #050508; /* 히어로/풋터 배경, 가장 짙은 레이어 */
  --off-white:    #F5F3EE; /* 밝은 섹션 배경, 본문 텍스트(다크 배경 위) */

  /* Accent */
  --gold:         #C9A24B; /* 주요 강조: 로고, 보더, CTA */
  --gold-light:   #E8D4A0; /* 호버, 서브 텍스트, 이탤릭 강조 */
  --gold-deep:    #8A6A1F; /* 라이트 배경 위 골드 텍스트/호버 (대비 보정용) */

  /* Semantic */
  --bg-primary:      var(--black);
  --bg-secondary:    var(--lapis-deep);
  --bg-tertiary:     var(--lapis-mid);
  --bg-inverse:      var(--off-white);

  --text-primary:    var(--off-white);
  --text-secondary:  rgba(245, 243, 238, 0.72);
  --text-inverse:    var(--lapis-deep);
  --text-accent:     var(--gold-light);

  --border-hairline: rgba(201, 162, 75, 0.25);
  --border-active:   var(--gold);

  /* State */
  --state-hover:      rgba(201, 162, 75, 0.10);
  --state-focus-ring: var(--gold-light);
  --state-error:      #B5654F; /* 저채도 테라코타 — 럭셔리 톤 유지 */
  --state-success:    #6F8F73; /* 저채도 세이지 */
}
```

**규칙**
- 골드는 전체 화면 면적의 5%를 넘지 않는다(라인·텍스트·아이콘 중심).
- 다크 배경(`--black`, `--lapis-deep`)에는 `--off-white` 또는 `--gold-light`만 텍스트로 사용, 순수 `#FFFFFF` 금지.
- 라이트 섹션(`--off-white` 배경)에서 골드 텍스트는 반드시 `--gold-deep`을 사용해 AA 대비를 확보한다.

---

## 4. Typography

```css
:root {
  --font-serif-en:  'Playfair Display', serif;   /* 영문 디스플레이 */
  --font-script-en: 'Cormorant Garamond', serif;  /* 영문 서브헤드/이탤릭 대체 */
  --font-serif-kr:  'Noto Serif KR', serif;       /* 국문 헤드라인 */
  --font-sans-kr:   'Pretendard Variable', -apple-system, sans-serif; /* 본문/UI */
}
```

| 스타일 | 서체 | 크기(desktop) | 자간 | 용도 |
|---|---|---|---|---|
| Display | Playfair Display 600 | clamp(52px, 10vw, 120px) | 0.18em | 로고형 헤드라인(LAPIS) |
| H1 (KR) | Noto Serif KR 500 | clamp(28px, 3.4vw, 42px) | 0.02em | 섹션 타이틀 |
| H2 (KR) | Noto Serif KR 500 | 24–28px | 0.02em | 서브 섹션 타이틀 |
| Eyebrow | Cormorant Garamond italic | 16–18px | 0.08em | 섹션 라벨(Brand Story 등) |
| Body | Pretendard 300 | 15–15.5px | 0 | 본문 문단, line-height 1.9–2.0 |
| Caption | Pretendard 400 | 12–13px | 0.1em | 캡션, 가격, 메타 정보 |
| Button/Nav | Pretendard 400 | 13px | 0.15–0.2em | CTA, 내비게이션 링크 |

**규칙**
- 국문은 이탤릭을 쓰지 않는다. 강조가 필요하면 자간(letter-spacing) 또는 컬러(`--gold-light`)로 대체한다.
- 한 화면에 서체는 최대 3종(영문 디스플레이 1 + 국문 세리프 1 + 산세리프 1)까지만 허용한다.
- 본문 line-height는 최소 1.7 이상 — 여백이 곧 브랜드 톤이라는 원칙과 연결된다.

---

## 5. Spacing

8px 베이스 스케일을 사용한다.

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --space-7: 48px;
  --space-8: 64px;
  --space-9: 96px;
  --space-10: 128px;
  --space-11: 160px;
}
```

| 용도 | 값(desktop) | 값(mobile) |
|---|---|---|
| 섹션 상하 패딩 | 120–140px (`--space-10` 내외) | 72–90px |
| 카드/컴포넌트 내부 패딩 | 40–56px | 28–32px |
| 그리드 gutter | 24–28px | 16px |
| 텍스트 블록 간 여백 | 16–28px | 12–20px |

원칙: 요소 간 간격은 항상 8의 배수. 예외는 헤어라인 보더(1px)와 아이콘 스트로크뿐이다.

---

## 6. Grid

- **컨테이너 최대 너비**: 1200–1280px
- **컬럼**: 데스크톱 12컬럼, 태블릿 8컬럼, 모바일 4컬럼
- **마진**: 데스크톱 6–8vw(최소 64px), 태블릿 40px, 모바일 24px
- **Gutter**: 24px(desktop) / 16px(mobile)

**Breakpoints**

| 이름 | 범위 | 비고 |
|---|---|---|
| mobile | < 600px | 1컬럼 스택, 내비 햄버거 전환 |
| tablet | 600–1023px | 2컬럼, 컬렉션 카드 1열 유지 가능 |
| desktop | 1024–1439px | 기준 레이아웃(3.x 절 기준) |
| wide | ≥ 1440px | 컨테이너 max-width로 여백만 증가, 컬럼 폭 고정 |

이미지 중심 섹션(히어로, 컬렉션 카드)은 그리드보다 `object-position` 우선순위 규칙을 따로 둔다 — 인물/제품의 시선/제품명 영역이 크롭에서 잘리지 않도록 앵커 포인트를 이미지별로 지정한다.

---

## 7. Components

### 7.1 Navigation
- 초기 상태: 투명 배경, `mix-blend-mode: difference`로 배경과 무관하게 가독성 확보
- 스크롤 40px 이상: 반투명 다크 배경 + blur, 패딩 축소(28px → 16px)
- 모바일(< 820px): 로고 + 메뉴 아이콘만 노출, 링크는 풀스크린 오버레이로 전환

### 7.2 Button / CTA
- **Primary(Outline)**: 1px 골드 보더, 투명 배경, 호버 시 배경 골드/텍스트 다크 반전(400ms)
- **Ghost(Text link)**: 밑줄 없음 → 호버 시 골드 밑줄이 왼쪽에서 오른쪽으로 draw-in
- 버튼은 절대 채워진 골드 배경으로 시작하지 않는다(항상 아웃라인 → 호버 시 반전)

### 7.3 Product / Note Card
- 3컬럼 그리드(탑/미들/베이스 노트), 호버 시 카드 배경이 `--lapis-deep`로 전환되며 텍스트가 `--off-white`로 반전
- 카드 내부 정렬: 넘버(eyebrow) → 영문명 → 국문명 → 구성 리스트 순, 항상 중앙 정렬

### 7.4 Collection / Line Card
- 이미지 풀블리드 + 하단 그라디언트 오버레이(`rgba(5,5,8,0) → rgba(5,5,8,.92)`)
- 정보(태그/제품명/설명/가격)는 하단 34px 패딩 안에 고정
- 호버 시 이미지 1.06배 스케일(1.2s ease), 텍스트는 고정(패럴랙스 없음)

### 7.5 Divider
- 좌우 60px 그라디언트 라인 + 중앙 골드 별(✦) 아이콘. 섹션 전환마다 반복되는 유일한 장식 요소

### 7.6 Form (Newsletter/Contact)
- 언더라인 인풋(보더 없음, 하단 1px 라인만), 포커스 시 라인이 골드로 전환
- 제출 후 메시지는 별도 토스트가 아니라 폼 하단 인라인 텍스트로 조용히 노출

### 7.7 Footer
- 로고 + 5개 이하 링크 + 저작권. 소셜 아이콘은 스트로크 1px, 최대 4개까지만

---

## 8. Motion

```css
:root {
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
  --duration-fast: 200ms;
  --duration-base: 400ms;
  --duration-slow: 800ms;
  --duration-cinematic: 1200ms;
}
```

| 상황 | Duration | Easing | 비고 |
|---|---|---|---|
| 호버(색/보더 전환) | 200–300ms | ease-in-out | 즉각적이되 급작스럽지 않게 |
| 스크롤 fade-up | 800–1000ms | ease-out | translateY(40px) → 0, opacity 0 → 1 |
| 이미지 스케일(호버) | 1000–1200ms | ease-out | 최대 1.06–1.08배까지만 |
| 섹션 간 전환(스크롤 스토리텔링) | 1200ms+ | ease-in-out | Margiela식 시네마틱 스크롤 참고 |
| 내비 숨김/노출 | 300ms | ease-out | 스크롤 다운 시 숨김, 업 시 노출 |

**금지 사항**: bounce, elastic, spring 계열 이징 전면 금지. 모든 모션은 "느리게 시작해서 느리게 끝난다"는 인상을 줘야 한다. `prefers-reduced-motion` 대응 시 fade-up은 즉시 표시로 대체(translate 제거, opacity만 0.3s).

---

## 9. Photography

- **조명**: 로우키 시네마틱. 웜 골드 키 라이트 1개 + 딥 블루 필 라이트. 하이라이트는 골드, 섀도우는 네이비로 크러시
- **컬러 그레이딩**: 암부는 딥 블루로 눌러주고, 하이라이트만 골드 톤으로 보정(과제 제작 노트의 "샤넬 광고 톤" 기준과 동일)
- **구도**: 제품은 매크로 클로즈업 또는 중앙 정렬 히어로샷, 인물은 여백을 넉넉히 두는 미디엄/풀샷. 정면 카탈로그 컷 지양
- **일관성**: 인물(뮤즈) 1인, 제품(사파이어 블루 병 + 골드 캡) 1종으로 캐릭터·제품 아이덴티티 고정
- **비율 가이드**: 히어로 3:4(모바일)/16:9(데스크톱), 컬렉션 카드 4:5, 스토리 섹션 자유 비율(단, 크롭 시 인물 시선 방향 여백 확보)
- **금지**: 과도한 비네팅, 스톡 사진 특유의 인위적 미소, 정면 플래시 조명

---

## 10. Iconography

- 스트로크 기반 라인 아이콘만 사용, 두께 1px(24px 그리드 기준), 채움(fill) 아이콘 금지
- 색상은 `--gold` 또는 `--off-white` 단색만 — 다색 아이콘 금지
- 브랜드 전용 심볼: 별(✦) — 청금석 안의 금빛 알갱이를 은유. 디바이더·로고 보조 요소로만 사용, 남용 금지(화면당 1회 권장)
- 사이즈 스케일: 16 / 20 / 24 / 32px

---

## 11. Interaction

- **호버**: 링크는 밑줄 draw-in, 이미지는 스케일업, 카드형 요소는 배경 반전 — 컴포넌트 타입별 호버 언어를 통일해 예측 가능하게 유지
- **스크롤 리빌**: IntersectionObserver 기준 threshold 0.15, 뷰포트 진입 시 1회만 트리거(재관찰 안 함)
- **포커스**: 키보드 포커스 시 `--state-focus-ring` 2px 아웃라인, 절대 `outline: none`만 적용하지 않음
- **커서(선택)**: 데스크톱 한정 커스텀 커서(작은 골드 도트)로 클릭 가능 영역을 은은히 표시 — 필수 아님, P2
- **폼**: 실시간 검증은 색보다 톤(차분한 인라인 문구)으로 안내, 에러 상태도 소리치지 않는다

---

## 12. Responsive Rules

- 타이포는 전 구간 `clamp()` 기반으로 유체 스케일링(브레이크포인트별 하드코딩 금지)
- 모바일에서 3컬럼 그리드(노트, 철학)는 1컬럼 스택, 카드 간 간격은 데스크톱의 약 60–70% 수준으로 축소
- 내비게이션은 820px 미만에서 링크 숨김 → 메뉴 아이콘 전환
- 이미지 크롭은 브레이크포인트별 `object-position` 프리셋을 따로 정의(인물 사진은 모바일에서 얼굴/제품 영역 우선)
- 터치 타깃 최소 44×44px, CTA 버튼 모바일 패딩은 14px×28px 이상 유지
- 히어로 섹션 높이는 모바일에서 `100dvh` 사용(주소창 높이 이슈 대응)

---

## 13. Accessibility

- 본문 텍스트 대비 최소 WCAG AA(4.5:1) 준수 — 다크 배경 위 `--off-white`, 라이트 배경 위 `--lapis-deep`/`--gold-deep` 조합만 본문에 사용
- 순수 장식 목적의 골드 텍스트(로고 등 대형 타이포)는 AA 예외(Large Text 기준 3:1)로 처리하되 배경 대비를 사전 검수
- 모든 편집 이미지에는 무드를 설명하는 alt 텍스트 제공(예: "군청빛 조명 아래 옆모습을 보이는 여성" 등 정보 손실 없이 서술)
- 포커스 인디케이터 항상 노출, 키보드만으로 내비게이션·폼·컬렉션 카드 접근 가능해야 함
- `prefers-reduced-motion` 대응: 패럴랙스·스케일 애니메이션 비활성화, 페이드만 최소한으로 유지
- 폼 요소에는 시각적 라벨 또는 `aria-label` 필수, placeholder만으로 라벨 대체 금지
- 본문 최소 폰트 크기 14px, 사용자 확대(pinch-zoom) 차단 금지

---

## 14. Design Do & Don't

| 구분 | Do | Don't |
|---|---|---|
| 컬러 | 골드는 포인트로만, 배경은 블루/블랙/오프화이트 3색 순환 | 골드를 배경 전체에 채우기, 채도 높은 원색 추가 |
| 타이포 | 서체 3종 이내, 국문/영문 위계 분리 | 화면당 4종 이상 서체 혼용, 국문 이탤릭 사용 |
| 여백 | 섹션당 최소 72px(모바일)/120px(데스크톱) 여백 확보 | 섹션 간 여백을 40px 이하로 좁혀 정보 밀도 높이기 |
| 모션 | 느린 fade/scale, 진입 1회성 리빌 | bounce/elastic 이징, 반복 재생되는 화려한 애니메이션 |
| 이미지 | 매크로/에디토리얼 컷, 여백 있는 구도 | 정면 카탈로그 컷, 과도한 보정/비네팅 |
| 카피 | 화면당 1문장, 침묵을 활용 | 설명적 장문 카피, 배너성 홍보 문구 남발 |
| 컴포넌트 | 아웃라인 CTA → 호버 시 반전 | 처음부터 채워진 화려한 그라디언트 버튼 |
| 아이콘 | 스트로크 단색, 별(✦) 모티프 절제된 반복 | 다색·채움 아이콘, 모티프 남용으로 장식화 |

---

*이 문서는 `lapis_storyboard_plan_source.md`(브랜드 기획서)와 기존 프로토타입(`index.html`)에서 사용된 컬러/타이포 토큰을 기준으로 확장 작성되었으며, Claude Design 목업 및 Claude Code 구현 단계에서 그대로 참조 가능하도록 구성했다.*
